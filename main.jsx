import React from "react";
import ReactDOM from "react-dom/client";
import PegadaDigital from "./PegadaDigital";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PegadaDigital />
  </React.StrictMode>
);
